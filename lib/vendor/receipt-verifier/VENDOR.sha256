# VENDORED FROM receipt-verifier — DO NOT EDIT IN PLACE.
#
# These are byte-for-byte copies of the shared verification core. A local edit here would be a
# second implementation wearing the first one's name, which is the exact failure 1423 came from:
# conformance had its own idea of what checking meant and it was weaker than prove's.
#
# test/vendor-parity.test.js re-derives these hashes, and compares them against the source repo
# when it is present beside this one. Absent, it says so and checks the pin only — an honest
# skip, never a silent pass.
#

# ── THE RECEIPT-VERIFIER CORE IS PINNED TO A RELEASED TAG ───────────────────────────────────
#
# The shared verification core here comes from receipt-verifier **v1.0.2**, peeled commit
# 51a8224439959a5b46c0b09e9a2cd67117f05d56.
#
# It was pinned to a WORKING TREE and to per-file commits. Both are honest about where the bytes
# came from and neither is a provenance a stranger can resolve: a working tree is not fetchable,
# so "the vendored bytes match upstream" reduced to "they match whatever is on this machine right
# now". A released tag is a name anyone can check out — that is the difference between a pin and
# a note.
#
# The hash lines below are UNCHANGED by the re-pin. Where a file already matched the tag this is
# prose only; where it did not, it was re-copied from the tag and that is recorded below.
#
#
# ── AND THE TAG IS SIGNED, WHICH IS THE PART A STRANGER CAN CHECK ───────────────────────────
#
# v1.0.0 was an unsigned tag: a name anyone can check out, and also a name anyone with push access
# can move. v1.0.1 is annotated and SSH-signed, so the pin now resolves to an identity and not
# only to bytes.
#
#   tag object   99567ea1c88b9b6e348118eeacd03592e3f1075f
#   peeled commit ac683b16c19662c9124c8cdab785223b28d2d0c6
#   signer       zsobpeter@gmail.com
#   fingerprint  SHA256:7yRXTm9zKGicfFpzL+7lpwFoPaoSwxAJlabB3jwxw2Y
#
# Verify it yourself, in a checkout of receipt-verifier:
#
#   git tag -v v1.0.2
#   -> Good "git" signature for zsobpeter@gmail.com with ED25519 key SHA256:7yRXTm9zKGicfFpzL+7lpwFoPaoSwxAJlabB3jwxw2Y
#
# WHY v1.0.2 AND NOT v1.0.1: the CORE IS BYTE-IDENTICAL between them — every hash line
# below is unchanged, and this re-pin moves none of them. v1.0.2 adds two things to the
# RELEASE: RECEIPT_FORMAT.md section 14 (the authority model) is in the tagged tree, and
# verification/allowed_signers ships in the repo so `git tag -v` names a principal on a
# machine that has never seen this project. A doc committed after a tag is not in that
# release, however committed it is — which is why the tag moved rather than the bytes.
#
# The peeled commit is UNCHANGED from v1.0.0 — the core is byte-identical and this re-pin moves no
# hash line. What it adds is who stands behind the bytes.
# source_commit: ac683b16c19662c9124c8cdab785223b28d2d0c6 (receipt-verifier v1.0.2, signed)
#

721fcac58b960b5e2aa1af91edb6cfc87ebe828a6da73879967ef79a1ca474e8  arity.js
348d3f851cd79bf63298b51da422d11be980820576ab0bf0a2990b9f007d6249  verify.js
4a0ad5bd7bb8282e086bea99dcaf8ca278370d80019c5681d2942bf11bc87504  verify-grant.js
3bb17bd4f16d5ee3cf3fbb754b4df9aa570a615ca94ff2a97b751c4fabd3bd0d  verify-prove-transcript.js
98cd9b989a25095e0442f91f68a1adecd6e6a2a5b05902bf2580b8cba973c5e2  verify-evidence.js
4d5476fc44bbfc2a847feeb313a1c6c08ef7fd832a2348ef3c244bd8d3f03fcd  evidence-root.js
bfe0c898cda3c3a4c8141726d0adb4cdb7eb9762de6264e430eff6469b90422e  keys/coderifts-keys.json
9d0259e916f2bcd32029744c3e9ec81448bcefa46e6ad3f054c8a650dc0482b7  verified-execution-binding.js
035105de0f163d112038624eb2bf93cee059a565216fd8b03489964f6b0b2fa8  verify-attest.js
